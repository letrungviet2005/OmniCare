# Dependencies for the one-video local pipeline.
-r voice_detection/requirements.txt
ultralytics>=8.3,<9
# Keep the base OpenCV wheel on the same supported major as the contrib wheel.
opencv-python>=4.8,<5
# Fall Detection uses the legacy mp.solutions API, removed in MediaPipe 0.10.30.
mediapipe>=0.10,<0.10.30
